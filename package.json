{
  "name": "xml-beautifier",
  "version": "0.3.0",
  "title": "XML Beautifier",
  "description": "Beautifies XML documents by putting each tag and text node on their own line and correctly indents everything",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --presets es2015",
    "prepublish": "npm run build",
    "test": "jest test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jonathanp/xml-beautifier.git"
  },
  "author": "Jonathan Persson <persson.jonathan@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-jest": "^21.2.0",
    "babel-preset-es2015": "^6.3.13",
    "jest": "^21.2.1",
    "tape": "^4.2.2"
  },
  "dependencies": {
    "repeat-string": "^1.5.2"
  },
  "jest": {
    "testMatch": ["**/test/*.js"]
  }
}
